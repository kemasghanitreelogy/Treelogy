{% paginate search.results by nil %}
  {% if search.performed %}
    <div id="search-results">
      {% if search.results.size > 0 %}
        {% for item in search.results limit: 4 %}
          {% unless item.object_type == 'product' and item.tags contains 'lp-only' %}
            {% render 'ProductCard', item: item %}
          {% endunless %}
        {% endfor %}
      {% else %}
        No results!
      {% endif %}
    </div>
  {% endif %}

  {% if paginate.pages > 1 %}{{ paginate | default_pagination }}{% endif %}
{% endpaginate %}
{% comment %}

This file has been rewritten to preserve the original behavior.

---
Changes:
- from: "{% paginate search.results %}"
  to: "{% paginate search.results by nil %}"
  position: 0..36
{% endcomment %}