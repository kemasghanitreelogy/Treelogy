{%- comment -%}
  Harga Lama — halaman placeholder (dummy). Semua teks lewat locale ns "harga_lama",
  tujuan tombol lewat routes.* supaya prefix /id tidak hilang.
{%- endcomment -%}
<section class="hl-body og-section">
  <div class="og-container hl-body__inner">
    <p class="hl-body__eyebrow">{{ 'harga_lama.eyebrow' | t }}</p>

    <h1 class="og-h2">{{ 'harga_lama.heading_html' | t }}</h1>

    <p class="og-lead hl-body__lead">{{ 'harga_lama.body_html' | t }}</p>

    <a class="hl-body__cta" href="{{ routes.all_products_collection_url }}">
      {{- 'harga_lama.cta_label' | t -}}
    </a>

    <p class="hl-body__note">{{ 'harga_lama.note' | t }}</p>
  </div>
</section>

{% style %}
  .hl-body {
    padding: calc(120 * var(--og-u)) 0 calc(140 * var(--og-u));
    font-family: var(--og-font);
  }

  .hl-body__inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: calc(40 * var(--og-u));
    text-align: center;
  }

  .hl-body__eyebrow {
    margin: 0;
    font-size: var(--og-fs-small);
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--og-accent);
  }

  .hl-body__lead {
    max-width: calc(820 * var(--og-u));
  }

  .hl-body__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: calc(28 * var(--og-u)) calc(70 * var(--og-u));
    border-radius: 999px;
    background: var(--og-ink);
    color: var(--og-cream);
    font-size: var(--og-fs-sub);
    font-weight: 600;
    line-height: 1;
    text-decoration: none;
  }

  .hl-body__note {
    margin: 0;
    font-size: var(--og-fs-small);
    opacity: 0.7;
  }
{% endstyle %}

{% schema %}
{
  "name": "HL Body",
  "settings": [],
  "presets": [
    {
      "name": "HL Body"
    }
  ]
}
{% endschema %}
