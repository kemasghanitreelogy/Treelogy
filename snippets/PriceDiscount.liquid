{% if compare_at_price != blank and compare_at_price > 0 and compare_at_price > current_price %}
  <span class="price-wrapper compare-at-price">
    {{ compare_at_price | money }}
  </span>
{% endif %}
